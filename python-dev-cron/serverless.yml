service: python-dev-cron

frameworkVersion: "3"

provider:
  name: aws
  runtime: python3.8

functions:
  rateHandler:
    handler: handler.run
    events:
      # Invoke Lambda function every minute
      - schedule: rate(10 seconds)
